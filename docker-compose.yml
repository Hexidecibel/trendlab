services:
  trendlab:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      # Host port 9393 matches the bin/update deployment; the app listens on 8000 inside
      - "9393:8000"
    environment:
      - DEBUG=${DEBUG:-false}
      - GITHUB_TOKEN=${GITHUB_TOKEN:-}
      - FOOTBALL_DATA_TOKEN=${FOOTBALL_DATA_TOKEN:-}
      - OPENAI_API_KEY=${OPENAI_API_KEY:-}
      - ANTHROPIC_API_KEY=${ANTHROPIC_API_KEY:-}
      - DATABASE_URL=sqlite+aiosqlite:///./data/trendlab.db
    volumes:
      # Persist SQLite database
      - trendlab-data:/app/data
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8000/health"]
      interval: 30s
      timeout: 10s
      start_period: 10s
      retries: 3

volumes:
  trendlab-data:
